---
---

No release: documentation, plus one comment-only addition to
`src/application/particle-system.ts` recording why the particle system does not
use `THREE.InstancedMesh`. No runtime behaviour, no exported type and no
packaged byte changes — consumers receive an identical build.
