---
---

No release: the change is one stale bullet in the top-level `README.md`, brought into agreement
with `docs/public-api.md` §1.3. Consumers receive identical bytes.

Worth recording for whoever bumps this next: the changeset gate exempts only `docs/` and
`.github/`, so a top-level `README.md` edit is NOT exempt even though it ships nothing — hence
this explicit no-release record rather than silence.
